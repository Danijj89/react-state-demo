import React, { useEffect } from 'react';
import { useCurrentUserStore } from './globalStates';


export const Zustand = () => {
    const { setCurrentUser } = useCurrentUserStore();

    useEffect(() => {

    })

    return (
        <>

        </>
    );
}